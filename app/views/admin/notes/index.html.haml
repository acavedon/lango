%h2 Listing notes

%table{:class => 'index_list'}
  %tr
    %th Title
    %th Body
    %th{:colspan => '3'} Actions

  - @notes.each do |note|
    %tr
      %td= note.title
      %td= note.body
      %td= link_to 'Show', [:admin,note]
      %td= link_to 'Edit', edit_admin_note_path(note)
      %td= link_to 'Destroy', admin_note_path(note), :confirm => 'Are you sure?', :method => :delete

%br

.actions
  = link_to 'New Note', new_admin_note_path
